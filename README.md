# Spindle
Spindle is an open-source runtime that lets coding agents explore multiple implementation branches in parallel, test them mid-run, and kill weaker paths early based on real execution results. It logs each run locally, learns your repo over time, and makes future issues faster, cheaper, and smarter.
